# Chenrui Zhou Personal Homepage

Static English personal homepage for GitHub Pages.

## Local preview

Open `index.html` directly in a browser.

## GitHub Pages deployment

For a user homepage, create a public repository named `<username>.github.io` and place these files at the repository root. GitHub Pages will serve the site at `https://<username>.github.io`.
